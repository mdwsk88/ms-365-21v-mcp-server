# MS 365-21V MCP Server — 用户使用说明

## 这是什么

**MS 365-21V MCP Server** 是一个面向企业内部的 MCP 智能网关，专为 21Vianet（世纪互联）中国云环境构建。它通过一个统一的连接地址，让 AI 客户端能够以员工身份安全地操作公司 Microsoft 365 数据——邮件、日历、OneDrive、SharePoint、Teams、联系人，以及未来更多的内部业务系统。

**核心特点：**

- **21V 原生**：所有认证和 API 端点默认走中国云，无需手动切换或额外配置
- **标准 OAuth 零配置接入**：支持 OAuth 2.1 的 MCP 客户端（Qoder Work、WorkBuddy 等）直接填地址就能连接，SSO 自动触发
- **按人分配工具**：不同部门、不同角色的员工，看到和能使用的工具不同——由 IT 管理员在 Entra 门户统一管控，MCP 无需重启
- **智能聚合**：不只是转发 API 数据，当前启用邮件摘要和日程冲突检测；Teams 消息聚合代码已实现，待租户批准频道消息读取权限后开放
- **敏感操作确认**：发邮件、删文件等高风险操作支持二次确认；客户端支持 MCP 表单时在客户端内完成，否则使用一次性网页预览
- **网关架构**：预留在同一个地址下接入 HR、ERP、审批等内部系统的能力；下游转发尚未实现

---

## 连接方式

在支持 OAuth 2.1 的 MCP 客户端中添加远程 MCP：

```text
https://your-mcp-host.example.cn/mcp
```

JSON 配置示例：

```json
{
  "mcpServers": {
    "ms365-pro": {
      "type": "streamable-http",
      "url": "https://your-mcp-host.example.cn/mcp"
    }
  }
}
```

- 类型：Streamable HTTP
- 认证：客户端自动触发 OAuth 2.1（无需手动填 client id 或 secret）
- 登录账号：公司 21V Microsoft 账号

---

## 首次登录

1. 在 MCP 客户端中添加上面的地址
2. 客户端自动弹出 Microsoft SSO 登录页面
3. 使用公司账号登录，并按公司策略完成 MFA 或设备验证
4. 登录成功后回到客户端，工具列表自动加载

如果浏览器没要求输入密码——这是正常的，说明 SSO 自动复用了浏览器已有的公司登录会话。

---

## 日常使用

连接成功后，直接用自然语言跟 AI 对话即可：

```
帮我看一下今天还有哪些会议
查一下我最近的未读邮件
帮我在 OneDrive 里搜索合同模板
在 SharePoint 项目清单中新增一条记录
给张三发一封邮件，主题是项目进度确认
帮我创建一个明天下午 3 点的会议
```

写操作（发邮件、建日程、上传文件等）以当前登录用户身份执行。如果管理员为该操作开启了确认，网关会先提供操作预览；客户端支持 MCP 表单时直接在客户端确认，否则会提供一次性网页预览链接。

---

## 可用工具范围

代码共实现 150 个工具。当前生产根据租户已获批权限隐藏 25 个工具，实际提供 **125 个工具**。用户无需记住工具名，直接用中文描述目标即可，Agent 会根据工具的英文名称和中英文场景描述自动匹配。

| 模块 | 工具数 | 可以做什么 |
|---|---:|---|
| 用户与组织资料 | 3 | 查本人和同事基础资料 |
| 组 | 1 | 检查本人是否属于指定组 |
| 邮件 | 23 | 查邮件、管理文件夹和附件、草稿、发送、回复、移动和删除 |
| 日历 | 17 | 查日程和忙闲、管理日历与会议、处理附件和邀请响应 |
| OneDrive | 18 | 搜索、下载、上传、移动、复制、分享和恢复文件版本 |
| SharePoint | 33 | 查询站点、管理 Lists/列/列表项、操作文档库文件和权限 |
| Teams | 10 | 查询已加入团队与频道基础信息，查询/创建聊天并收发聊天消息 |
| 联系人 | 11 | 查询和管理联系人及其文件夹 |
| Microsoft Search | 5 | 对邮件、日历、文件、SharePoint 和 Teams 做全文搜索 |
| 智能聚合 | 2 | 邮件摘要和日程冲突分析 |
| 网关基础 | 2 | 查看认证状态、继续执行已由同一用户批准的操作 |

完整工具名、读写属性、所需角色和 Graph 权限见 [Tool Catalog](docs/TOOL_CATALOG.md)。具体能看到哪些模块由管理员分配的 Entra App Role 决定。

管理员还可以选择工具暴露模式。默认 `direct` 让 Agent 直接调用每个具体工具；`hybrid` 会保留常用工具，同时通过“搜索工具 → 查看参数 → 执行工具”访问长尾能力。两种方式最终都执行同一个受权限和审计保护的业务工具，用户仍然只需要用中文描述目标，不需要自己记住动态工具名。

---

## 智能聚合工具

除了标准的增删改查工具，本项目实现了三个智能工具。当前生产启用其中两个；它们会对多条 Graph 数据做结构化聚合、排序或冲突计算，让一次提问直接得到更容易使用的结果。当前邮件工具不是调用大模型阅读全文生成语义摘要，涉及具体正文时仍可继续读取选中的邮件。

### 邮件智能摘要 (`smart_mail_digest`)

**做什么**：快速了解"最近有哪些重要邮件需要关注"，不用逐封翻阅。

**怎么用**：
```
帮我看看今天有什么重要邮件
总结一下最近 24 小时的未读邮件
有没有带附件的邮件需要我处理
```

**返回内容**：邮件总数、未读数、带附件数、按"内部/外部"分类汇总、重要邮件列表（发件人 + 主题 + 时间）。

### 日程冲突检测 (`smart_calendar_conflicts`)

**做什么**：扫描未来几天的日程，找出时间冲突的会议，告诉你哪些时段还空闲。

**怎么用**：
```
帮我看看明天有没有日程冲突
这周我还有哪些空闲时间可以安排会议
检查一下未来 3 天的会议有没有撞车的
```

**返回内容**：冲突列表（哪两个会议重叠、重叠多少分钟）、忙碌率、空闲时段建议。

### Teams 未读聚合 (`smart_teams_unread`)

**当前状态：暂未开放。** 该工具依赖组织级 `ChannelMessage.Read.All` delegated permission；代码已经实现，但在租户权限获批前不会出现在 MCP 工具列表中。

**做什么**：汇总所有 Teams 频道和私聊的近期消息，让你快速了解"哪些对话需要关注"，不用逐个频道翻。

**怎么用**：
```
帮我看看 Teams 上有什么需要关注的
汇总一下各个频道的最近讨论
我休假回来了，Teams 上有什么新消息
```

**返回内容**：标注频道或聊天来源、发件人、正文预览、提及数和附件数的近期消息候选，并按关注优先级排序。

> 注意：Microsoft Graph 没有统一的"未读计数"接口，因此返回的是"近期消息列表"而非严格的未读消息，结果中会标注这一限制。

---

## MCP 网关：一个地址，连接所有系统

### 为什么是"网关"

本项目不只是 Microsoft 365 的工具集。它的架构设计是一个可扩展的网关。当前已经提供内置 M365 工具；下游 MCP 的配置格式和扩展点已经预留，但工具发现和调用转发尚未实现：

```
你（自然语言对话）
       ↓
┌─────────────────────────────────────┐
│  MS 365-21V MCP Server（网关）       │
│   统一认证 · 角色过滤 · 审计日志    │
└──┬──────────┬──────────┬────────────┘
   ↓          ↓          ↓
 M365       HR 系统    未来...
 邮件/日历   考勤/请假   ERP/审批/Wiki
 文件/Teams
```

**目标体验**：下游转发实现后，用户地址、操作方式和登录方式保持不变，新系统能力会以带前缀的工具出现在同一工具列表中。

### 后续如何接入新业务系统

1. 业务系统的开发者提供独立的 Streamable HTTP MCP 服务
2. 在网关中为下游配置唯一 ID、地址和工具名前缀
3. 实现下游 `tools/list` 发现、缓存和 `tools/call` 转发
4. 确定用户 Token 透传、Token exchange 或服务身份等认证模式
5. 在 Entra ID 中定义对应的新角色（如 `mcp.hr`），并把下游工具绑定到该角色
6. 把敏感写操作接入统一确认、审计、超时和故障隔离
7. 完成身份、权限和转发测试后重新发布网关

当前仅填写 `MCP_DOWNSTREAM_SERVICES` 不会产生新工具。等代理转发和动态重载能力实现后，才能做到新增下游时不修改客户端配置。

---

## 工具权限：为什么我看到的工具和别人不一样

### 机制

本项目采用**按角色动态过滤**：根据你在 Entra ID 中被分配的角色，实时决定你能看到和使用哪些工具。

- 张三（HR 部门）：看到邮件 + 日历 + 未来的 HR 工具
- 李四（IT 部门）：看到邮件 + 日历 + Teams + SharePoint + OneDrive
- 王五（管理员）：看到当前租户权限已放行的全部工具

**权限随新 Token 生效**——IT 管理员在 Entra 门户分配已有角色后，不需要重启 MCP 服务；但旧 Token 不会被修改。用户应在客户端重置或重新授权 MCP，让 Entra 签发包含新角色的新 Token，然后刷新工具列表。

### 角色与工具模块对应关系

| Entra App Role | 控制的工具模块 |
|---|---|
| `mcp.mail` | 邮件（23 个工具） |
| `mcp.calendar` | 日历（17 个工具） |
| `mcp.drive` | OneDrive（18 个工具） |
| `mcp.sharepoint` | SharePoint（33 个工具） |
| `mcp.teams` | Teams（当前 10 个工具；另有 16 个待 Graph 权限批准） |
| `mcp.contacts` | 联系人（11 个工具） |
| `mcp.users` | 用户与组织资料（当前 3 个工具） |
| `mcp.groups` | 组查询（当前 1 个工具） |
| `mcp.search` | Microsoft Search（需配合对应数据域角色） |
| `mcp.smart` | 智能聚合（需配合对应数据域角色） |
| `mcp.admin` | 当前租户权限已放行的全部工具 |

`mcp.admin` 只能放开当前部署已经加载且 Graph scope 已获批的工具，不会绕过 Microsoft Graph delegated permissions、用户对目标数据的实际访问权或敏感操作确认。

**租户 Graph scope 边界**：当前有 13 项组织级 delegated permission 尚未进入应用注册或未完成同意，因此依赖它们的 25 个工具被部署配置隐藏。IT 后续获批权限时，需要先在 `21V-Graph-MCP-API` 中添加并完成 consent，再由运维从 `MCP_DISABLED_GRAPH_SCOPES` 移除对应 scope 并重新部署。仅给用户分配 App Role 不会恢复这些工具。

**智能工具的组合授权**：使用智能工具需要同时拥有 `mcp.smart` + 对应数据域角色。例如：
- 邮件摘要 → `mcp.smart` + `mcp.mail`
- 日程冲突 → `mcp.smart` + `mcp.calendar`
- Teams 聚合 → 当前还需先批准 `ChannelMessage.Read.All`，开放后再分配 `mcp.smart` + `mcp.teams`

**全文搜索的组合授权**：使用搜索工具需要 `mcp.search` + 对应数据域角色。例如搜索文件需要 `mcp.search` + `mcp.drive`，搜索邮件需要 `mcp.search` + `mcp.mail`。

### 给用户配置权限（IT 管理员操作步骤）

**第 1 步：登录 Entra 门户**

访问 https://portal.azure.cn → Microsoft Entra ID

**第 2 步：找到 MCP 应用**

左侧菜单 → Enterprise applications → 搜索 "21V-Graph-MCP-API" → 点击进入

**第 3 步：添加用户/组分配**

左侧菜单 → Users and groups → 点击 "Add user/group"

**第 4 步：选择目标**

- 给整个部门配权限：选择一个 Security Group（如"HR 部门"组）
- 给个人配权限：直接搜索用户名

**第 5 步：选择角色**

在 "Select a role" 中选择一个要开放的角色：
- 选择 `mcp.mail` → 该用户可使用邮件工具
- 选择 `mcp.admin` → 该用户可使用当前部署加载的全部工具
- 同一个用户/组需要多个角色时，完成一次 Assign 后重复新增分配

**第 6 步：保存**

点击 Assign 完成。为确保新角色进入 Token，让用户在客户端中重置或重新授权该 MCP，然后重新登录并刷新工具列表。

### 如果需要新增角色（接入新系统时）

1. 进入 Entra 门户 → App registrations → 找到 "21V-Graph-MCP-API"
2. 左侧 → App roles → Create app role
3. 填写：Display name（如"HR 系统使用者"）、Value（如 `mcp.hr`）、Allowed member types = Users/Groups
4. 保存后回到 Enterprise Application → Users and groups 分配该角色

新增 App Role 本身不会自动产生新工具。网关还必须实现对应模块或下游转发，并建立工具到该角色的映射。

---

## 敏感操作确认

管理员可独立配置“发送消息”和“其他敏感写操作”：

- `MCP_SEND_MODE=confirm`：默认模式，发送邮件和 Teams 消息前要求用户确认。
- `MCP_SEND_MODE=automatic`：邮件和 Teams 消息立即发送，适用于经过审批的无人值守自动化。它不会绕过登录、Graph 权限、Entra App Roles 或审计。
- `MCP_CONFIRM_OPERATIONS`：继续控制删除、分享、成员移除、创建日程等其他写操作。即使配置为 `all`，发送行为仍以 `MCP_SEND_MODE` 为准。

这是服务端部署策略，不是 Agent 可以在一次对话中自行切换的参数。需要交互确认和无人值守自动发送同时存在时，建议部署两个使用不同配置和不同授权人群的 MCP 实例，避免普通用户误用自动发送通道。

**体验**：你让 AI "帮我发一封邮件给 XXX"时——

1. 客户端支持 MCP form elicitation 时，AI 在客户端内展示邮件预览和确认选项
2. 用户接受后立即发送；拒绝、取消或未勾选均不执行
3. 客户端不支持表单时，AI 返回一次性 HTTPS 预览链接
4. 用户在网页检查并批准后回到客户端，Agent 调用 `confirm_execute` 完成发送

网页确认令牌与当前登录用户绑定、短时有效且只能执行一次。`confirm_execute` 只能继续执行已在网页批准的操作，不能自行授予批准。

---

## 常见问题

**工具列表能看到，但调用报 Missing bearer token**
→ 授权状态失效。在 MCP 设置中重置连接，重新 SSO 登录即可。

**登录时没弹密码框**
→ 正常。SSO 复用了浏览器已有的公司会话。

**我看到的工具比同事少**
→ 通常由 IT 管理员通过 Entra 角色控制；也可能是该工具依赖的组织级 Graph permission 尚未获批。先联系 IT 确认角色，再由运维检查生产 scope 边界。

**操作失败显示 "requires role 'mcp.xxx'"**
→ 你没有该模块的使用权限。联系 IT 管理员在 Enterprise Application → Users and groups 中分配角色。

**操作失败但没提到 role**
→ 可能你本身没有目标资源的访问权（如不是某 Teams 团队成员），或租户管理员还没给 MCP 应用授予对应 Graph permission。

**写操作弹出确认，能跳过吗**
→ 不能。这是管理员配置的合规要求。

---

*MS 365-21V MCP Server — 企业级 MCP 智能网关*

---
